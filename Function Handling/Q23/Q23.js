


//23. Write a js program to find sum of elements of array using function.

let sumArry = function(arr){
    sum = 0;
    for(let i = 0; i < array.length ; i++ ){
        sum = sum + array[i];
        
    }
    
    return sum
}

let array = [ 1,2,3,4,5]
console.log( "Sum of Arry Elements are " +sumArry(array))